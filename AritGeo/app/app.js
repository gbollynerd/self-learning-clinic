'use strict'

module.exports = {

 /* Find the average of three integers */
	aritGeo : function(){
  		if (arguments.length === 0){
    		return 0;
  		}

  		if (arguments.length <= 2){
    		return -1;
  		}
    
    
	    
  	}
	  return -1;
	}
}